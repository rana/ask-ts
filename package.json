{
  "name": "ask-ts",
  "version": "0.1.0",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "bin": {
    "ask": "./src/cli.ts"
  },
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build --compile --target=bun src/cli.ts --outfile=ask",
    "test": "bun test",
    "type-check": "bun tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock": "^3.940.0",
    "@aws-sdk/client-bedrock-runtime": "^3.941.0",
    "chalk": "^5.3.0",
    "citty": "^0.1.5",
    "zod": "^4.1.13"
  }
}